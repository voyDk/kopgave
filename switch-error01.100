/* switch-error01.100 */
/* Repeated case label */

int main()
{
  int x;
  x = getint();
  switch (x) {
    case 42: putstring("Life, universe and everything.");
    case '*': putstring("*");
    default : putint(0);
  }
  return 0;
}
